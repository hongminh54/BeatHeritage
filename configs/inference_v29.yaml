defaults:
  - base_inference
  - inference
  - osut5: train_v29
  - diffusion: v1
  - _self_

model_path: 'OliBomby/Mapperatorinator-v29.1'
diff_ckpt: 'OliBomby/osu-diffusion-v2'
version: 'BeatHeritage V1'        # Beatmap version
output_type: [TIMING, MAP]    # Output type (map, timing)
super_timing: true    # Use super timing generator (slow but accurate timing)
timing_leniency: 5     # Number of milliseconds of error to allow for timing generation
